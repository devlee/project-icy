# Studio（后台创作管理）

> 占位。UI 开发暂缓——先完成设计（见 `docs/product-design.md` §2，设计 skill 见 `.cursor/skills/frontend-design/`）。
>
> 技术栈已定：Next.js 全栈（App Router + Server Actions），Node 编排 ComfyUI，伴生 worker 进程跑长任务。
> 依赖 `@icy/core`（数据模型/流水线）与 `@icy/adapters`（存储/生成/队列）。
